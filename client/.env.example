# Production build (Vercel) — see client/.env.production
# Or set VITE_API_URL in Vercel → Environment Variables (overrides .env.production)

# Local dev: leave empty — Vite proxies /api to http://localhost:3000
VITE_API_URL=
